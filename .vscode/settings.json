{
    // This file defines workspace-specific settings for running LaTeX build tasks.
    // Use the tasks defined in `tasks.json` for building LaTeX documents.
    // You can run tasks like "Build with pdflatex" or "Open Gummi for live preview"
    // manually from the VS Code task runner (Ctrl+Shift+B) or via the Command Palette.
    //
    // Automatically run the default LaTeX build task (pdflatex) when saving a .tex file.
    
    // VS Code will trigger the default build task on save
    "tasks.autoDetect": "on",
    "task.autoBuild": {
        "files.watcher": "**/*.tex",
        "task": "Build with pdflatex"  // Automatically run pdflatex on save
    }
}